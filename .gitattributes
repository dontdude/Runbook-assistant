# Keep Unix executables LF-only even on Windows checkouts (core.autocrlf=true).
# CRLF gradlew breaks the Docker Linux build with "./gradlew: not found" (exit 127).
gradlew text eol=lf
gradle/wrapper/gradle-wrapper.properties text eol=lf
*.sh text eol=lf
gradlew.bat text eol=crlf
